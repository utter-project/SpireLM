import "uservars_asr_spire.tconf"
import "asr_constants.tconf"
import "models_spire.tconf"
import "dsu_spire.tconf"

global {

    # paths relative to $repo, defined in a uservars file
    template=(
        Template:
            spire_style="instruction-templates/spire-style-template.json"
    )
}
